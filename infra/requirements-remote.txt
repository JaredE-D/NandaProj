# Installed on the vast.ai box, on top of the image's existing CUDA + torch.
# We deliberately do NOT pin torch here -- the image supplies a torch that
# matches its CUDA driver, and overriding it is the fastest way to break CUDA.

# Jupyter side. jupyter-collaboration is required: the MCP server reads
# notebook state through Jupyter's real-time-collaboration layer.
jupyterlab>=4.2,<5
jupyter-collaboration>=3.0
jupyter-mcp-tools
ipykernel
ipywidgets

# Interpretability.
# jlens (the Jacobian lens) is the critical path -- SPEC.md L12 -- and is not on
# PyPI, so it installs from source. It requires transformers>=5.5, which
# transformer-lens/sae-lens likely do not tolerate; those are deliberately off
# the critical path, so jlens wins the version fight.
jlens @ git+https://github.com/anthropics/jacobian-lens.git
transformer-lens>=2.0
nnsight>=0.3
sae-lens>=4.0

# Modelling. transformers/huggingface_hub arrive transitively via the libraries
# above, but PLAN.md loads them directly, so they are pinned as direct deps.
# >=5.5 is jlens's floor, not ours.
transformers>=5.5
huggingface_hub>=0.26

# Probes and stats (PLAN.md 5.2/5.4/5.5): logistic + ridge probes, the
# covariance-matched null, and the V9 Spearman dose-response test.
scikit-learn
scipy

# Supporting
datasets
einops
jaxtyping
plotly
circuitsvis
pandas
pyarrow          # parquet, for the 02 behavioral tables
tqdm

# Lets the idle watchdog destroy its own instance.
vastai>=1.5
